---
# handlers file for ansible_role_ansible_role_cis_centos

- name: restart tmp.mount
  become: yes
  systemd:
    name: tmp.mount
    daemon_reload: yes
    enabled: yes
    masked: no
    state: reloaded
